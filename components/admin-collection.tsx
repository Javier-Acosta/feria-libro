"use client";
import { FormEvent, useEffect, useState } from "react";
type RecordItem = Record<string, unknown> & { id: string; title?: string; name?: string; published?: boolean; guests?: string[] };
type Field = { name: string; label: string; type: "text" | "textarea" | "date" | "time" | "file"; required?: boolean };
const definitions: Record<string, Field[]> = { news:[{name:"title",label:"Título",type:"text",required:true},{name:"summary",label:"Resumen",type:"textarea",required:true},{name:"content",label:"Contenido",type:"textarea",required:true},{name:"published_on",label:"Fecha",type:"date",required:true},{name:"image",label:"Imagen",type:"file"}], guests:[{name:"name",label:"Nombre",type:"text",required:true},{name:"participation",label:"Participación",type:"text"},{name:"bio",label:"Biografía",type:"textarea",required:true},{name:"photo",label:"Fotografía",type:"file"}], schedule_entries:[{name:"event_date",label:"Fecha",type:"date",required:true},{name:"event_time",label:"Hora",type:"time",required:true},{name:"title",label:"Actividad",type:"text",required:true},{name:"venue",label:"Ubicación",type:"text",required:true}], venue_maps:[{name:"title",label:"Título",type:"text",required:true},{name:"file",label:"Archivo (imagen o PDF)",type:"file",required:true}] };
export function AdminCollection({ collection, title }: { collection:string; title:string }) { const [records,setRecords]=useState<RecordItem[]>([]);const [guests,setGuests]=useState<RecordItem[]>([]);const [editing,setEditing]=useState<RecordItem|null>(null);const [message,setMessage]=useState("");const load=async()=>{const r=await fetch(`/api/admin/${collection}`);if(r.ok)setRecords(await r.json());if(collection==="schedule_entries"){const g=await fetch("/api/admin/guests");if(g.ok)setGuests(await g.json())}};// eslint-disable-next-line react-hooks/set-state-in-effect
useEffect(()=>{void load()},[collection]);async function save(event:FormEvent<HTMLFormElement>){event.preventDefault();setMessage("");const form=new FormData(event.currentTarget);form.set("published",form.get("published")?"true":"false");const r=await fetch(`/api/admin/${collection}${editing?.id?`/${editing.id}`:""}`,{method:editing?.id?"PATCH":"POST",body:form});if(!r.ok){const result=await r.json();return setMessage(result.message??"No se pudo guardar.")}setEditing(null);event.currentTarget.reset();setMessage("Cambios guardados.");await load()}async function remove(id:string){if(!confirm("¿Eliminar este contenido?"))return;const r=await fetch(`/api/admin/${collection}/${id}`,{method:"DELETE"});if(r.ok){setMessage("Contenido eliminado.");await load()}}const fields=definitions[collection];return <div className="content-manager"><div className="manager-title"><div><p className="eyebrow blue">GESTIÓN</p><h1>{title}</h1></div><button className="secondary" onClick={()=>setEditing({id:"",published:false})}>+ Nuevo</button></div>{message&&<p className="manager-message">{message}</p>}{(editing||!records.length)&&<form className="content-form" onSubmit={save}><h2>{editing?.id?"Editar contenido":"Nuevo contenido"}</h2>{fields.map(field=><label key={field.name}>{field.label}{field.type==="textarea"?<textarea name={field.name} required={field.required} defaultValue={String(editing?.[field.name]??"")}/>:<input name={field.name} type={field.type} required={field.required&&field.type!=="file"} defaultValue={field.type!=="file"?String(editing?.[field.name]??""):undefined}/>}</label>)}{collection==="schedule_entries"&&<fieldset><legend>Disertantes / invitados</legend>{guests.length?guests.map(guest=><label className="check" key={guest.id}><input name="guests" type="checkbox" value={guest.id} defaultChecked={Boolean(editing?.guests?.includes(guest.id))}/>{guest.name}</label>):<span>Primero cargá invitados para asociarlos.</span>}</fieldset>}<label className="check"><input name="published" type="checkbox" defaultChecked={Boolean(editing?.published)}/>Publicar en el sitio</label><div className="form-actions"><button>{editing?.id?"Guardar cambios":"Crear contenido"}</button>{editing&&<button type="button" className="secondary" onClick={()=>setEditing(null)}>Cancelar</button>}</div></form>}<div className="record-list">{records.map(record=><article key={record.id}><div><span className={record.published?"status published":"status"}>{record.published?"Publicado":"Borrador"}</span><h3>{record.title??record.name}</h3></div><div className="record-actions"><button onClick={()=>setEditing(record)}>Editar</button><button onClick={()=>void remove(record.id)}>Eliminar</button></div></article>)}</div></div> }
